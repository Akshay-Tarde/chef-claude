export default function Main() {
    return (
        <main>
            <form action="submit">
                <input type="text" placeholder="e.g.Oregano" />
                <button>+ Add ingredient</button>
            </form>
        </main>
    )
}